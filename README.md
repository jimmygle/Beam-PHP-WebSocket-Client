# Beam.pro PHP Web Socket Client

## Overview

Currently only increments the viewers of specified channel, but handles authentication.

## Install
`composer install`
Update config in index.php

## Use
`php index.php`